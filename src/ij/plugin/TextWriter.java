package ij.plugin;import ij.*;import ij.process.*;import ij.io.*;import ij.text.*;import java.awt.*;/** *  This plugin implements the File/Save As/Text command. What it does is save *  the contents of TextWindows (e.g., "Log" and "Results"). * *@author     Thomas *@created    3 decembre 2007 */public class TextWriter implements PlugIn {	/**	 *  Main processing method for the TextWriter object	 *	 *@param  arg  Description of the Parameter	 */	public void run(String arg) {		saveText();	}	/**	 *  Description of the Method	 */	void saveText() {	Frame frame = WindowManager.getFrontWindow();		if (frame != null && (frame instanceof TextWindow)) {		TextPanel tp = ((TextWindow) frame).getTextPanel();			tp.saveAs("");		} else {			//EU_HOU Bundle			IJ.error("Save As Text", "This command requires a TextWindow such as\n"					 + "the \"Log\" window or an \"Info for...\" window.");		}	}}