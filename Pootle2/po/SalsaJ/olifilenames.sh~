#!/bin/bash
# olifilenames.sh

if [ $1 == "" ]
then
 extens="_ro"
else
 extens="_"$1
fi

for file in `ls $extens/*.po`
do
 newname=`basename $file $extens.po`
 echo ">>>File:" $newname 
 mv $file $newname
 chmod 644 $newname
done
